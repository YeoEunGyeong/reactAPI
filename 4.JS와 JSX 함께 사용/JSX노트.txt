JSX란?

리액트의 장점은 리렌더링 ; 비교에 대한 알고리즘
엘리먼트 타입이 바뀌면 이전 엘리먼트를 버리고 새로 그림
엘리먼트 타입이 동일하면 ??
; key를 먼저 비교함
props(DateSet: [], {}, [{}])를 비교해서 변경사항만 새로 그림

바닐라 JS ; 변경이 발생하면 무조건 다시 그림 (전체 모든 것을 새로 그림)
React는 변경된 부분만 다시 그림 (효과적!)
→ 변경된 부분을 알기 위해서는 계속 비교해야 한다는 단점

React는 불변 객체 (immutable)
; ReactDOM.render 함수로 element(JSX)로 전달만 함
변경에 대한 판단은 리액트가 스스로 함 - 변경사항에 대한 반영은 리액트의 일